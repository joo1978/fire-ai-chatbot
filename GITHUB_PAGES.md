# GitHub Pages 공개 시제품 배포

이 저장소는 `main` 브랜치에 반영될 때 GitHub Actions가 정적 웹앱을 빌드해 GitHub Pages에 배포하도록 구성되어 있습니다.

## 최초 1회 설정

1. GitHub에 새 저장소를 만들고 이 프로젝트를 업로드합니다.
2. 저장소의 `Settings → Pages`로 이동합니다.
3. `Build and deployment → Source`를 `GitHub Actions`로 선택합니다.
4. `Actions` 탭의 `GitHub Pages 배포` 작업이 성공했는지 확인합니다.

공개 주소는 일반 프로젝트 저장소의 경우 다음 형식입니다.

`https://<GitHub 아이디>.github.io/<저장소명>/`

## 시제품의 기술적 범위

- 브라우저에서 실행되는 반응형 단일 화면 웹앱입니다.
- 12개 검증 시연답변과 분야별 탐색·키워드 검색을 제공합니다.
- 별도의 데이터베이스, 로그인, 생성형 AI API는 사용하지 않습니다.
- 입력한 질문은 서버로 전송하지 않고 현재 브라우저에서 비교합니다.
- 실제 운영 전 최신 법령과 대상물 현황을 별도로 확인해야 합니다.

## 정식 서비스 전환 시 추가할 사항

- 기관 인증과 사용자 권한관리
- 보안 서버와 접속기록 관리
- 최신 법령·지침·질의회신 데이터베이스
- 검색증강생성(RAG) 및 AI API
- 답변 근거 원문 연결과 최신성 검증
- 개인정보·민감정보 입력 통제와 보안성 검토
